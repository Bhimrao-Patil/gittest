create one folder GitTest
create txt file in that folder
add some statement on that txt file
pwd
git init --it initialise the repository
before that need to minimum configuration for 1st timegit
  git config --global user.name "Bhimrao-Patil"
  git config --global user.email "bypatil04@gmail.com"test
  otherwise we can not push and pull the code or add or commit the code
git config user.name
git config user.email 
7 and 8 step for verification purpose
 git config --list --all data shown
 git init --for empty repo 
 .git hidden file is created after than so dont change or add into that directory. it contains all info that is necessary for the project and all info 
realating commits, remote repo address. It is also contains a log that stores the commit history. This can help we to rool back to the desired version of the code
git status ->we can see the txt file in red color
git add filename
git status -> we can see that txt file in green color
git commit -m "msg"
But it is now local repo not in remote repo
git status  ->it cant show any file becuase it is strored
git init
git log --it shows the msgs and who and when the chages are done
if we have 1000 logs we want to checl what you did the commit last
  git log --help
  git log --oneline or
  git log --online -2
  git log --author="Bhimrao-Patil" --oneline
  git log --since=2-27-2022
  git log --since=2-27-2022 --oneline
  git log --author="Bhimrao-Patil"
  git log --grep="change" --basically checks the commit msges
 
